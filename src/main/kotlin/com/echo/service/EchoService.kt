package com.echo.service

/**
 * Implements the behavior.
 */
class EchoService {

    /**
     * Echoes the input message by returning the same value.
     *
     * @param message The input message to be echoed.
     * @return The same message that was input.
     */
    fun echo(message: String) = message
}
